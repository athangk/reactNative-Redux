import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  checkBtn: {
    marginTop: 8,
    width: 44,
    height: 44,
    backgroundColor: "#48B7BB",
    borderRadius: 12,
    justifyContent: "center",
    alignItems: "center",
  },
  removeBtn: {
    backgroundColor: "red",
  },
})
